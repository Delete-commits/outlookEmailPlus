# 待办事项（Backlog）

> 最后更新：2026-03-03  
> 说明：用于记录已完成需求后的遗留问题、技术债与下一步优化项（按优先级排序）。

---

## TODO-00005｜多邮箱统一管理 — 遗留问题/技术债

### 总体评价

P0 主链路已完成并通过全量单测回归（导入/导出/邮件列表/详情/验证码提取/IMAP 禁删/调度任务跳过 IMAP）。  
当前主要风险集中在 **IMAP(Generic) 列表拉取的性能** 与 **前后端 provider 展示的单一数据源**。

### 具体问题列表（按严重程度排序）

1) **IMAP(Generic) 邮件列表拉取性能/带宽风险（高）**  
- 现状：`outlook_web/services/imap_generic.py` 列表为生成 `body_preview` 与 `has_attachments`，对每封邮件使用 `UID FETCH ... RFC822` 拉取整封邮件。  
- 影响：遇到大邮件/多邮件时列表加载显著变慢；后端 CPU/内存/网络开销偏大；可能触发 IMAP 服务端限流/断连。  
- 关联文件：`outlook_web/services/imap_generic.py`

2) **IMAP 账号“编辑/刷新”链路未适配（中）**  
- 现状：前端编辑账号校验仍强制 `client_id/refresh_token`；Repo 的 `update_account()` 也要求非空，IMAP 账号不满足。  
- 影响：IMAP 账号导入后若需修改授权码/服务器配置，缺少一致的编辑入口（只能删除重导）。  
- 关联文件：`static/js/features/accounts.js`、`outlook_web/repositories/accounts.py`、`outlook_web/controllers/accounts.py`

3) **provider label 存在“双维护”（低）**  
- 现状：后端 `MAIL_PROVIDERS` 定义 label；前端 `getProviderLabel()` 又硬编码一份。  
- 影响：后续新增/修改 provider 文案容易不一致。  
- 关联文件：`outlook_web/services/providers.py`、`static/js/features/groups.js`

4) **IMAP 连接超时设置的全局副作用（低）**  
- 现状：为兼容旧版本 `imaplib.IMAP4_SSL`，在不支持 `timeout=` 参数时会调用 `socket.setdefaulttimeout(30)`（全局）。  
- 影响：可能影响进程内其他网络连接的默认超时语义，增加排查成本。  
- 关联文件：`outlook_web/services/imap_generic.py`

### 改进建议和示例代码

#### A) IMAP(Generic) 列表拉取改为“轻量 FETCH”（建议优先）

目标：列表接口只拉取必要信息（FLAGS + HEADER + 预览片段），避免整封 RFC822。  

建议方向（伪代码）：

```python
# 仅抓取必要字段：FLAGS + 头部 + BODYSTRUCTURE + 正文前 N 字节（按需）
mail.uid(
    "FETCH",
    uid,
    "(FLAGS BODY.PEEK[HEADER.FIELDS (SUBJECT FROM DATE)] BODYSTRUCTURE BODY.PEEK[TEXT]<0.2048>)",
)
```

验收标准（建议写入任务卡）：
- 列表接口不再使用 `RFC822` 拉取整封邮件；
- `body_preview` 来自 partial fetch（或仅 header+subject 预览兜底）；
- `has_attachments` 使用 `BODYSTRUCTURE`/part 判断（或降级为“未知/False”并在详情再判定）。

#### B) IMAP 账号编辑能力（P1）

建议：
- 后端为 IMAP 账号新增更新接口或在 `update_account()` 增加 `account_type` 分支校验；
- 前端编辑模态框按 `account_type/provider` 动态展示字段（授权码/host/port）。

#### C) provider label 单一来源

建议：
- 账号列表 tag 展示优先使用 `GET /api/providers` 返回的 `label`（由前端缓存映射），移除硬编码 `getProviderLabel()` 或仅作为兜底。

---

## TODO-00008｜对外验证码与邮件读取开放 API — 实施待办/开发清单

### 总体评价

`PRD / FD / OpenAPI / TDD / TEST` 已完成，且针对新能力的专项测试已先行落地：

- `tests/test_external_api.py`
- `tests/test_settings_external_api_key.py`
- `tests/test_verification_extractor_options.py`
- `tests/test_ui_settings_external_api_key.py`（新增：前端设置页 external_api_key 集成验证）

当前项目已经进入**按测试驱动实现代码**阶段。现状不是“需求不清”，而是“实现尚未补齐”：

- ✅ 开放接口 `/api/external/*` 已注册并实现（统一响应结构 + 审计打点）
- ✅ `external_api_key` 设置项已接入（加密存储 + 脱敏回显）且 `api_key_required()` 已实现
- ✅ 参数化提取函数 `extract_verification_info_with_options()` 已实现

验证通过：

- `python -m unittest tests.test_settings_external_api_key -v`
- `python -m unittest tests.test_verification_extractor_options -v`
- `python -m unittest tests.test_external_api -v`
- `python -m unittest discover -s tests -v`（256 tests）

执行日志：`docs/LOG/LOG-00008-对外验证码与邮件读取开放API-执行日志.md`

### 具体问题列表（按严重程度排序）

1) **开放 API 鉴权与配置链路未落地（高）— 已完成**  
- 现状：`/api/settings` 已支持 `external_api_key` 的保存、脱敏与状态回显；`api_key_required()` 已实现。  
- 影响：开放接口已形成独立于后台登录态的调用闭环。  
- 关联文件：`outlook_web/controllers/settings.py`、`outlook_web/repositories/settings.py`、`outlook_web/security/auth.py`、`outlook_web/db.py`

2) **开放系统接口未实现（高）— 已完成**  
- 现状：`/api/external/health`、`/api/external/capabilities`、`/api/external/account-status` 已实现。  
- 影响：本地化部署接入自检与能力探测已可用。  
- 关联文件：`outlook_web/routes/system.py`、`outlook_web/controllers/system.py`

3) **验证码/验证链接参数化提取能力未实现（高）— 已完成**  
- 现状：已新增 `extract_verification_info_with_options()`，支持 `code_regex`、`code_length`、`code_source` 等可配置入口。  
- 影响：开放接口已可覆盖更多平台注册邮件模板，减少接入方二次解析成本。  
- 关联文件：`outlook_web/services/verification_extractor.py`

4) **开放邮件读取接口未实现（高）— 已完成**  
- 现状：`/api/external/messages`、`/latest`、`/{message_id}`、`/{message_id}/raw`、`/verification-code`、`/verification-link`、`/wait-message` 已落地。  
- 影响：开放 API 主链路已可用。  
- 关联文件：`outlook_web/routes/emails.py`、`outlook_web/controllers/emails.py`、`outlook_web/services/external_api.py`

5) **统一错误码/审计日志闭环未完成（中）— 已完成**  
- 现状：开放接口统一返回结构（`success/code/message/data`）与审计打点已收口。  
- 影响：外部调用调试与观测口径一致。  
- 关联文件：`outlook_web/controllers/emails.py`、`outlook_web/controllers/system.py`、`outlook_web/services/external_api.py`

6) **前端设置页与手工验收路径待补齐（低）— 已完成**  
- 现状：设置页已增加 `external_api_key` 的展示与保存入口（仅脱敏回显，避免占位符写回 DB）。  
- 影响：本地部署可直接在 UI 完成对外 API Key 配置与禁用，无需手改数据库。  
- 关联文件：`templates/index.html`、`static/js/main.js`、`outlook_web/controllers/settings.py`

### 改进建议和实施顺序

#### A) 第一阶段：先打通配置与鉴权（P0，建议优先）

目标：让开放接口具备最小可用的认证闭环。

实施项：

- 在 `outlook_web/db.py` 为 `settings` 初始化 `external_api_key`
- 在 `outlook_web/repositories/settings.py` 增加：
  - `get_external_api_key()`
  - `get_external_api_key_masked()`
- 在 `outlook_web/controllers/settings.py`：
  - `GET /api/settings` 返回 `external_api_key_set`、`external_api_key_masked`
  - `PUT /api/settings` 支持保存 `external_api_key`
- 在 `outlook_web/security/auth.py` 新增 `api_key_required()`，仅接受 `X-API-Key`

建议先跑：

```bash
python -m unittest tests.test_settings_external_api_key -v
```

验收标准：

- 设置接口可保存开放 API Key
- 设置读取时不回显明文
- 未配置/缺失/错误 API Key 的开放接口行为与测试预期一致

#### B) 第二阶段：补开放系统自检接口（P0）

目标：提供本地化部署后的接入探测能力。

实施项：

- 在 `outlook_web/routes/system.py` 注册：
  - `GET /api/external/health`
  - `GET /api/external/capabilities`
  - `GET /api/external/account-status`
- 在 `outlook_web/controllers/system.py` 实现统一返回结构与状态码

建议先跑：

```bash
python -m unittest tests.test_external_api.ExternalApiAuthTests -v
python -m unittest tests.test_external_api.ExternalApiSystemTests -v
```

验收标准：

- 正确 API Key 可通过系统自检接口
- 返回字段与 OpenAPI 草稿一致（`capabilities.version`、`account-status.can_read` 等）
- `account-status` 能区分账号存在/不存在/不可用等状态（含 `account_type/provider/preferred_method`）

#### C) 第三阶段：补参数化提取器（P0）

目标：让验证码与验证链接提取逻辑满足外部接入场景。

实施项：

- 在 `outlook_web/services/verification_extractor.py` 新增 `extract_verification_info_with_options()`
- 支持：
  - `code_regex`
  - `code_length`
  - `code_source`
  - 高置信验证链接优先级（`verify` / `confirm` / `activate` / `validation`）

建议先跑：

```bash
python -m unittest tests.test_verification_extractor_options -v
```

验收标准：

- 默认行为不破坏旧提取能力
- 自定义正则和长度范围都能生效
- 链接优先返回高置信验证链接

#### D) 第四阶段：补开放邮件与自动化接口（P0）

目标：完成开放 API 主业务链路。

实施项：

- 建议新增 `outlook_web/services/external_api.py`，统一封装：
  - 账号读取
  - Graph → IMAP(New) → IMAP(Old) 回退
  - 列表筛选
  - 最新邮件定位
  - 详情读取
  - `wait-message` 轮询
- 在 `outlook_web/routes/emails.py` 注册：
  - `GET /api/external/messages`
  - `GET /api/external/messages/latest`
  - `GET /api/external/messages/<message_id>`
  - `GET /api/external/messages/<message_id>/raw`
  - `GET /api/external/verification-code`
  - `GET /api/external/verification-link`
  - `GET /api/external/wait-message`
- 在 `outlook_web/controllers/emails.py` 落地统一响应、参数校验与审计打点

建议先跑：

```bash
python -m unittest tests.test_external_api -v
```

验收标准：

- 邮件列表、详情、RAW、验证码、验证链接、等待邮件均可调用
- 返回结构统一为 `success/code/message/data`
- Graph 与 IMAP 回退链路不影响旧内部接口

#### E) 第五阶段：做回归封板与手工验收（P1）

目标：确认“新接口上线、旧接口零回归”。

实施项：

- 跑专项测试：

```bash
python -m unittest tests.test_external_api -v
python -m unittest tests.test_settings_external_api_key -v
python -m unittest tests.test_verification_extractor_options -v
```

- 跑全量回归：

```bash
python -m unittest discover -s tests -v
```

- 按 `docs/TEST/TEST-00008-对外验证码与邮件读取开放API-测试文档.md` 执行一次手工联调

验收标准：

- 新增测试全部转绿
- 旧接口相关回归测试通过
- 文档、OpenAPI、真实实现三者保持一致

### 建议里程碑

| 里程碑 | 目标 | 完成标志 |
|---|---|---|
| M1 | 配置与鉴权闭环 | `test_settings_external_api_key` 通过 |
| M2 | 系统自检可用 | Auth + System 相关测试通过 |
| M3 | 提取器参数化完成 | `test_verification_extractor_options` 通过 |
| M4 | 开放主链路可用 | `test_external_api` 通过 |
| M5 | 回归封板 | `discover -s tests` 通过 |

### 风险提示

- **兼容性风险**：开放接口新增时，不能改坏现有内部接口 URL 与返回结构。
- **安全风险**：不得在日志、设置页返回、异常栈中暴露明文 `external_api_key`。
- **实现风险**：若 controller 内继续堆逻辑，后续维护成本会快速上升，建议抽出 `external_api.py` service。
- **测试风险**：`wait-message` 涉及时间轮询，必须通过 `mock sleep` 保证测试稳定、快速。
